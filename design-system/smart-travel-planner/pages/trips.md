# Page: Trips (list + details)

- List cards are **cinematic cover cards**: blurred country-flag backdrop, sharp flag chip,
  white title overlay, glass-backed status badge. Whole card is the link; chevron nudges on hover.
- Details page: cover strip header with avatar-style flag straddling the boundary.
- Budget bars: gradient primary→primary-light, amounts `tabular-nums`.
- Filters use `.field`; table view keeps flat rows (no covers, no stagger).
